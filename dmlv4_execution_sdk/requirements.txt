# DMLV4 执行进度回传SDK 依赖

# 核心依赖
aiohttp>=3.8.0
pydantic>=2.0.0

# 开发依赖 (可选)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# 示例依赖 (可选)
# pytest>=7.0.0  # 用于examples