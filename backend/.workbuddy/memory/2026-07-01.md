# 2026-07-01 工作日志

## 后端代码评审
- 对 DML V4 Backend (FastAPI + MongoDB + Redis + Kafka/RabbitMQ + MinIO) 进行全面代码评审
- 评审范围：316 个 Python 文件，13 个业务模块，shared 基础设施层
- 产出评审报告：`code_review_report.html`（综合评分 60/100）
- 发现 7 个 P0 严重问题、14 个 P1 高危问题、12 个 P2 中等问题

### 已修复的 P0 问题
- **AI 客户端** (shared/ai/client.py): `OpenAI` → `AsyncOpenAI`，同步 → 异步 API
- **RabbitMQ 生产者** (shared/rabbitmq/producer.py): 新增 `send_task_async()` 异步包装 + `asyncio.Lock` 保护重连
- **AI 连接测试** (system_config/service/config_service.py): `asyncio.to_thread` 包装同步调用
- **Redis 管理 API** (shared/api/routes/redis.py): set/del/publish 端点增加 `require_permission("system:config")`
- **execution_plan 路由鉴权** (execution_plan/api/routes.py): 全模块补齐 `execution_tasks:read/write` 权限校验，修复 `get_case_execution_stats` 完全匿名问题
- **project 模块 UserDoc bug** (project/service/project_service.py): 3 处 UserDoc 查询移除不存在的 `is_deleted` 字段
- **project 路由鉴权** (project/api/routes.py): create/update/delete/generate-demo-data 增加 `require_permission("test_cases:write")`
- **P0 安全**：config.yaml + config_dev.yaml 含明文密钥(JWT/RabbitMQ/MinIO/Redis)且被 git 跟踪；多个模块路由缺失鉴权(execution_plan/execution agent/project/ai_analysis/system_config)；Redis 管理 API 任意登录用户可操作任意 key
- **P0 稳定性**：AI 客户端(shared/ai/client.py)用同步 OpenAI 阻塞事件循环；RabbitMQ 生产者同步阻塞且线程不安全
- **P0 数据**：project 模块查 UserDoc 误用 is_deleted 字段导致 owner 名称永远解析失败
- **P1 性能**：N+1 查询、$regex 全表扫描、全量加载内存、Embedding 内存余弦计算、AI 无流式响应
- **设计亮点**：日志系统、Kafka 死信处理、异常分层、路由自动注册、notification 持久化恢复、test_case_collection 分层

### P0 修复代码自审
- RabbitMQ 修复补全：task_dispatcher.py 的 `_dispatch_via_rabbitmq` 改为 `async def`，调用 `send_task_async`，`dispatch` 用 `await` 等待，P0 阻塞真正解决
- project routes.py 装饰器行 149-198 字符，超 110 限制（flake8 已 ignore E501）
- AI client.py `chat_completion` 的 `timeout` 参数原代码就未使用，未引入也未修复
- execution_plan 的 READ_DEP/WRITE_DEP 模块级列表用法正确
- project 权限码借用 test_cases:write（无 projects:write 定义），语义不完全匹配
- Web: FastAPI + Beanie(MongoDB ODM) + motor
- MQ: Kafka(kafka-python) + RabbitMQ(pika 同步/aio-pika)
- AI: OpenAI(同步,需改异步) + LangChain + LangGraph
- 存储: MinIO + Redis(Sentinel)
- 认证: 自实现 JWT(HS256) + RBAC
- 模块分层: api/service/repository/domain/schemas

### 前端优化
根据登录日志（13:34）分析发现的重复请求问题，优化前端代码：
- **AuthProvider 防重**：新增 `_loginInProgress` ref，防止 handleLoginSuccess 设置 isAuthenticated 后 useEffect 恢复逻辑重复触发，从根源消除 users/me ×2 和 permissions ×2
- **AuthProvider 暴露 currentUserData**：新增 `currentUserData` context 字段，ProfilePage 等组件可直接消费，免去重复 API 请求
- **ProfilePage 复用 context**：改从 useAuth() 获取已有数据，仅在 context 未就绪时 fallback 到 API；添加 `initialDataLoaded` ref 防重
- **DashboardPage → React Query**：5 个 API 调用从 `useEffect + useState` 迁移到 `useQueries`，利用全局 30s staleTime 缓存去重
- **Vite Proxy**：vite.config.ts 添加 `server.proxy: /api → localhost:8801`；.env 改为 `/api/v1` 相对路径，消除开发环境 15+ 个 CORS OPTIONS 预检请求
