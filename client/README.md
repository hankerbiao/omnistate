# Fake Client（已废弃）

> **注意**：平台任务下发已改为仅支持 **RabbitMQ**。本目录下的 HTTP 假执行框架不再与当前后端对接。

联调请使用后端脚本：

```bash
cd backend
python scripts/mock_test_framework.py
```

详见 [测试执行编排](../docs/guide/test-execution.md)。
